import dirtyChai from 'dirty-chai';
import chai, { expect } from 'chai';
import StripeClient from '../src/StripeClient';

chai.use(dirtyChai);

describe('StripeClient Tests', () => {
  describe('api definition', () => {
  });
});
